# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? |
|---|------|-------|----------|--------|-----------|------------|
| D001 | M001 | arch | Backend platform | Convex (keep from template) | Already wired, realtime built-in, serverless | No |
| D002 | M001 | arch | Auth provider | Clerk (keep from template) | Already wired on both platforms | No |
| D003 | M001 | convention | Weight storage unit | kg internally, display per user preference | International standard, avoids rounding errors on conversion | No |
| D004 | M001 | arch | Exercise data source | Curated JSON seed (~100-150 exercises) | User decided against external API. Seed loaded via Convex mutation. | Yes — if community library (R025) changes sourcing |
| D005 | M001 | scope | Primary user loop | Freeform workout logging (no structured programs) | User explicitly chose freeform. Programs deferred (R026). | Yes — if R026 is activated |
| D006 | M001 | scope | Tracking depth | Full: weight, reps, RPE, tempo, notes per set | User explicitly requested full tracking | No |
| D007 | M001 | convention | Design language | Clean/minimal, light theme, Apple Health-inspired | User chose over dark/gym and vibrant/energetic | No |
| D008 | M001 | arch | Rest timer behavior | Auto-start after logging a set, configurable per exercise | User confirmed. Timer state is local (not synced to Convex). | No |
| D009 | M001 | scope | Platform targets | Both web + mobile from M001 | User explicitly wants both platforms from the start | No |
| D010 | M001 | scope | Offline support | Deferred — nice to have, not M001 | Convex lacks built-in offline. Significant custom work. | Yes — revisit after M002 |
| D011 | M001/S01 | arch | Schema normalization strategy | Fully normalized 7 tables with v.id() references | Convex doc-based but workout data is relational. Normalized keeps docs small, enables M002 analytics queries, avoids transaction limits on nested docs. | No |
| D012 | M001/S01 | convention | Muscle group storage | primaryMuscleGroup as indexed string, secondaryMuscleGroups as display-only array | Convex can't index array elements for equality queries. Primary is indexed for filter queries; secondary is for display. | No |
| D013 | M001/S01 | convention | Exercise query dual-path | withSearchIndex for text search, withIndex for filter-only browse | Convex search index requires a text query. Pure filter queries use regular indexes. Two code paths in listExercises. | No |
| D014 | M001/S01 | convention | Seed idempotency | Check by slug via by_slug index before each insert | Per-exercise slug check is more robust than count-based check — handles partial seed failures. | No |
| D015 | M001/S01 | convention | Filter UI components | Native HTML select elements, no headless UI dependency | Matches clean/minimal design language (D007). Avoids adding a component library dependency for simple dropdowns. Can upgrade to Radix Select later if needed. | Yes |
| D016 | M001/S01 | convention | Exercise type enum expansion | Added `plyometric` as 5th exercise type alongside strength/cardio/bodyweight/stretch | Needed for box jumps, jump squats, etc. in seed data. Schema and seed data both include it. | No |
| D017 | M001/S02 | convention | S02 verification strategy | Internal test helpers (`convex/testing.ts`) using `internalMutation`/`internalQuery` with explicit `testUserId` arg to bypass Clerk auth for programmatic testing | ConvexHttpClient can't provide Clerk auth tokens. Internal functions are not exposed to client API. Same pattern as `seed.ts`. | Yes — if Convex adds test auth support |
| D018 | M001/S02 | convention | Active workout resumption | UI checks for existing inProgress workout via `getActiveWorkout` before creating new one | Prevents duplicate active workouts. Schema doesn't enforce uniqueness, so UI enforces it. | No |
| D019 | M001/S02 | convention | Duration computation | `durationSeconds` computed server-side in `finishWorkout` as `Math.round((completedAt - startedAt) / 1000)` | Server-side computation is authoritative. Client timer is display-only. | No |
| D020 | M001/S02 | convention | WorkoutCard data fetching | WorkoutCard self-fetches exercise count via `useQuery(listExercisesForWorkout)` rather than receiving it as a prop | Avoids N+1 prop drilling from parent. Convex handles reactive subscriptions efficiently — each card subscribes independently. | Yes |
| D021 | M001/S02 | convention | Set input pattern | SetRow uses local state for weight/reps with `onBlur` save — weight conversion at input boundary (`lbsToKg` on blur, `displayWeight` on render) | Avoids mutation spam on every keystroke. Server always stores kg (D003). Conversion happens at component boundary only. | No |
| D022 | M001/S02 | convention | Double-creation guard | ActiveWorkout uses `useRef` flag to prevent duplicate workout creation during React strict mode double-renders | React 18+ strict mode double-invokes effects in dev. Without the guard, two workouts would be created on mount. | No |
| D023 | M001/S03 | convention | Superset UX approach | Explicit "Create Superset" button with checkbox selection, not drag-to-group | Simpler implementation, fits clean/minimal design language (D007). Drag-to-group is more discoverable but significantly more complex. Can upgrade later. | Yes |
| D024 | M001/S03 | convention | Previous performance query strategy | Multi-table traversal: workoutExercises (by_exerciseId) → workouts (filter userId + completed) → sets. No denormalization. | O(N) where N = total workoutExercise rows for that exercise. Acceptable for M001 single-user scale. Revisit if >100ms in practice. | Yes |
| D025 | M001/S03 | convention | RPE validation | Server-side rejection (throw error) for RPE values outside 1-10 range | Don't rely on client-side validation alone. Mutation rejects invalid values with descriptive error message. | No |
| D026 | M001/S03 | convention | Tempo input format | Freeform string, no structured parsing | Users may enter "3-1-2-0", "3120", or descriptive text. Accept any string for S03; structured input can come later for analytics. | Yes |
| D027 | M001/S03 | convention | Previous performance formatting | Consecutive identical sets grouped with count prefix ("3×10 @ 60 kg"), unit-aware via formatWeight | Reduces visual noise vs listing every set individually. Groups like sets (same weight+reps) into "N×reps @ weight" notation. | No |
| D028 | M001/S03 | convention | Superset visual grouping | 6-color rotating left-border palette with discriminated union render items (single vs superset) | Visually distinct superset containers with color-coded borders. Discriminated union cleanly separates render logic for grouped vs ungrouped exercises. | No |
| D029 | M001/S03 | convention | Set notes UI pattern | Collapsible row below main set row via toggle icon, auto-opens if notes exist | Avoids cramping the main set row with a text field. Notes are optional and infrequent — hide by default. | No |
| D030 | M001/S04 | convention | Superset rest timer behavior | Timer starts after any set in a superset, same as single exercises (no shared timer logic) | R005 mentions "grouped exercises share rest timers" but exact UX is ambiguous. Simplest safe interpretation: each set log triggers the timer regardless of superset membership. Shared superset timer (only after last exercise in round) adds medium complexity for unclear UX benefit. Can upgrade later. | Yes |
| D031 | M001/S04 | convention | Rest duration priority chain | workoutExercise.restSeconds → exercise.defaultRestSeconds → userPreferences.defaultRestSeconds → 60s hardcoded fallback | Four-level priority gives users control at every granularity. Exercise override is most specific, user pref is global default, 60s is universal fallback. Duration of 0 means "no timer" (stretches, cardio). | No |
| D032 | M001/S04 | convention | Timer accuracy technique | `Date.now()` arithmetic from stored endTime, not counter decrement | Browser throttles setInterval in background tabs. Computing remaining from wall clock ensures timer shows correct time when tab regains focus. Interval runs at 100ms for smooth visual updates. | No |
| D033 | M001/S05 | convention | Template targetSets/targetReps derivation | `targetSets = actual set count`, `targetReps = first set's reps` (or undefined) | Simple deterministic mapping from completed workout data. First set's reps is the most likely "target" rep count. Avoids complex heuristics like mode or average. | Yes |
| D034 | M001/S05 | convention | Start-from-template creates workout without pre-created sets | Workout exercises are added but no empty set rows — user logs sets fresh | R006 says templates leave weight/RPE blank for fresh input. Pre-creating empty sets adds complexity and stale rows if user changes set count. User adds sets via the existing logSet flow. | No |
| D035 | M001/S05 | convention | Active workout conflict on start-from-template | Reject with descriptive error if user already has an inProgress workout | Safest approach — prevents accidental orphaned workouts. User must finish or delete existing workout first. Consistent with D018 (ActiveWorkout auto-resume). | No |
| D036 | M001/S05 | convention | Superset grouping not preserved in templates | `templateExercises` lacks `supersetGroupId` — superset info lost on save | Minor gap. Adding the field requires a schema change for low-priority benefit. Templates capture exercise selection and structure, not workout flow details. Can add later. | Yes |
| D037 | M001/S06 | arch | Mobile navigation pattern | Bottom-tab navigator with nested stacks per tab, auth-gated via `useAuth().isSignedIn` | Matches gym usage (one-thumb tab switching). Auth state drives navigation automatically instead of manual post-OAuth navigation. `@react-navigation/bottom-tabs` already in package.json. | No |
| D038 | M001/S06 | convention | Mobile filter UI pattern | Horizontal scrollable chip/pill selectors instead of HTML `<select>` or RN Picker | Native-feeling filter interaction for mobile. More touch-friendly than pickers. Fits clean/minimal design (D007). | Yes |
| D039 | M001/S06 | convention | Mobile timer completion feedback | `Vibration.vibrate()` from react-native core instead of Web Audio API | Web Audio doesn't exist in React Native. Vibration is the natural mobile equivalent. expo-haptics can be added later for richer feedback. | Yes |
| D040 | M001/S06 | convention | Mobile prompt replacement | Custom `TextInputModal` component with title, TextInput, Cancel/Confirm buttons | `window.prompt()` doesn't exist in React Native. Reusable modal serves template naming and any future text input needs. | No |
| D041 | M001/S06 | convention | Shared utilities approach | Copy `units.ts` to `apps/native/src/lib/` instead of creating a shared package | Avoids adding a new monorepo package for 60 lines of pure functions. Simple copy. Both copies are identical and framework-independent. | Yes |
| D042 | M001/S06 | convention | Mobile circular timer display | View-based half-circle rotation approach instead of `react-native-svg` Circle | `react-native-svg` is not in project dependencies. View-based approach avoids adding a dependency for a single visual element. Two half-circle overlays with rotation transforms provide adequate circular progress display. | Yes |
| D043 | M002 | arch | Analytics computation strategy | On-demand aggregation with dedicated query functions, no pre-computed rollup tables | Single-user scale (hundreds of workouts, ~4K sets max) is well within Convex 10s query timeout and 16K doc read limit. Pre-computed tables add complexity (scheduled functions, consistency) without proven need. Revisit if queries exceed 2s. | Yes |
| D044 | M002 | arch | PR storage approach | Dedicated `personalRecords` table storing detected PRs, checked inside `logSet` | Querying only the PR table for current bests (not scanning all historical sets) keeps PR detection fast on the mutation path. PRs persist across sessions and power exercise history views. | No |
| D045 | M002 | convention | Estimated 1RM formula | Epley formula: `weight × (1 + reps/30)`. For 1-rep sets, actual weight = 1RM. Skip estimation for sets > 15 reps. | Well-established strength training standard. Simple one-liner. Edge case handling prevents inaccurate estimates at extremes. | Yes |
| D046 | M002 | arch | Chart library strategy | Recharts (web) + Victory Native XL (mobile) — separate implementations, shared data shape contract | Best-in-class for each platform. No cross-platform chart abstraction — the data shape from Convex queries is the shared contract, chart components are fully platform-specific. | No |
| D047 | M002 | convention | Muscle heatmap implementation | Custom SVG component (web) + react-native-svg (mobile) with shared body outline SVG asset | No off-the-shelf muscle heatmap library exists. SVG body outline with 9 color-coded regions is ~200 LOC. Reverses D042's avoidance of react-native-svg — heatmap with 9 regions justifies the dependency. | No |
| D048 | M002 | convention | Analytics time periods | Standard periods: 30d, 90d, 6mo, 1yr, all-time. Default to 90d. | Covers common fitness tracking windows. Client sends period, server computes boundaries. | Yes |
| D049 | M002 | convention | PR types | Three PR types: weight PR (heaviest single set), volume PR (most total volume in one exercise session), rep PR (most reps at a given weight) | R012 explicitly lists all three. Each has distinct detection logic and user value. | No |
| D050 | M002 | convention | Slice ordering — web first, mobile port last | S01-S03 deliver backend + web UI. S04 ports all analytics to mobile. | Mirrors M001/S06 pattern. Isolates Victory Native XL + Skia risk to one slice. Backend queries are shared — mobile only needs chart/heatmap components. | No |
| D051 | M002/S01 | convention | logSet return type change | `logSet` returns `{ setId, prs }` instead of bare `setId`. `testLogSet` keeps returning bare `setId` for backward compat; new `testLogSetWithPR` returns `{ setId, prs }`. | Adding PR info to return value enables callers to show instant feedback. Keeping old test helper signature avoids breaking M001 verify scripts. | No |
| D052 | M002/S01 | convention | PR detection error handling | PR detection in `logSet` is wrapped in try/catch — failures logged but don't break set logging | Set logging is the primary user action; PR detection is secondary. A bug in PR code must never prevent a user from recording their workout. | No |
| D053 | M002/S01 | convention | Rep PR simplification | Rep PR stores "most reps in a single working set regardless of weight" — no per-weight-tier tracking | Simpler storage (one rep PR per exercise, not per weight tier). Less precise but avoids schema complexity. Can refine later per S01-RESEARCH open risk. | Yes |
| D054 | M002/S01 | convention | Volume PR evaluated on every set, not just at workout completion | Volume PR running total is compared against stored PR on each set log, not deferred to workout finish | Enables realtime 🏆 badge during workout. The running total uses already-fetched sibling sets array. | No |
| D055 | M002/S01 | convention | PR badge query strategy | Single `useQuery(getWorkoutPRs)` per exercise card, filtered client-side by exerciseId | Avoids N+1 subscriptions — one query per card returns all PRs for the workout (typically 0-5), filtered in `useMemo`. Workouts rarely have many PRs so client-side filter is negligible. | Yes |
| D056 | M002/S01 | convention | PR badge animation | Custom `@keyframes pr-badge-in` in globals.css instead of adding tailwindcss-animate | Project doesn't have tailwindcss-animate installed. One keyframe doesn't justify a new dependency. | Yes |
| D057 | M002/S01 | convention | Agent-testable UI attributes | `data-pr-badge` attribute on PR badge container for programmatic verification | Allows `document.querySelectorAll('[data-pr-badge]')` in browser assertions without fragile class/text selectors. Pattern reusable for future UI observability. | No |
| D058 | M002/S02 | convention | Exercise progress chart data shape | `getExerciseProgress` returns `{ date: number, maxWeight: number, totalVolume: number, estimated1RM?: number }[]` sorted by date ascending, one entry per completed workout session | This is the S02→S04 boundary contract. Mobile chart (Victory Native XL) consumes the same data shape. `estimated1RM` is optional because sets >15 reps skip Epley estimation. | No |
| D059 | M002/S02 | convention | Exercise detail page route | `/exercises/[id]` dynamic route with ExerciseCard Link wrapper for navigation | New Next.js route. Middleware already protects `/exercises(.*)`. ExercisePicker uses its own inline rendering — adding Link to ExerciseCard is safe. | No |
| D060 | M002/S02 | convention | Chart dual Y-axis strategy | Left axis for weight/1RM (same kg scale), right axis for volume (different magnitude) | Volume (weight×reps) is 10-50x larger than max weight. Single axis makes weight lines flat. Dual axes keep all metrics readable on one chart. | Yes |
| D061 | M002/S02 | convention | Export estimateOneRepMax for reuse | `estimateOneRepMax` exported from `convex/lib/prDetection.ts` instead of duplicated in `analytics.ts` | Single source of truth for the Epley formula. Analytics and PR detection use the same function. | No |
| D062 | M002/S03 | convention | Secondary muscle group volume attribution | 50% volume attribution for secondary muscle groups in heatmap; primary-only volume as main metric in bar chart | Provides more accurate heatmap coloring (bench press lights up triceps/shoulders too). Bar chart shows simpler primary-only to avoid confusion. Heatmap uses percentages which normalizes the total. | Yes |
| D063 | M002/S03 | convention | SVG heatmap path data location | `packages/backend/data/muscle-heatmap-paths.ts` — framework-agnostic file (no React/JSX) | Both web and native already have `@packages/backend` path alias. Avoids creating a new `packages/shared` monorepo package for one file. S04 mobile imports from same location. | Yes |
| D064 | M002/S03 | convention | Analytics dashboard data attributes | `data-analytics-heatmap`, `data-analytics-barchart`, `data-analytics-summary-weekly`, `data-analytics-summary-monthly` on dashboard sections | Extends D057 pattern for programmatic UI verification. Each dashboard section independently detectable by CSS selector. | No |
| D065 | M002/S03 | convention | Period summary shared compute function | `computePeriodSummary(db, userId, periodDays)` shared by weekly (7d) and monthly (30d) summaries | DRY — both summaries have identical aggregation logic, only the time window differs. Single function, two thin query wrappers. | No |
| D066 | M002/S03 | convention | Analytics dashboard period options differ from exercise detail | Dashboard uses 7d/30d/90d/All Time (supersedes D048 for dashboard context). Exercise detail keeps 30d/90d/6mo/1yr/All Time. | Analytics dashboard benefits from shorter default windows (7d) for weekly cadence visibility. Exercise progress needs longer windows to show meaningful trend lines. D048 still applies to exercise detail page. | Yes |
| D067 | M002/S04 | convention | Mobile analytics component naming | Native analytics components suffixed with `Native` (e.g. `MuscleHeatmapNative`, `VolumeBarChartNative`) | Avoids name collisions with web components in monorepo-wide searches. Makes platform clear at import site. Mobile components are fully platform-specific — no shared React code with web. | Yes |
| D068 | M002/S04 | convention | Mobile chart height convention | Victory Native XL charts use explicit 280px height in parent View (no ResponsiveContainer equivalent) | Victory Native XL requires explicit dimensions — no auto-sizing. 280px balances readability with screen real estate on standard phone viewports. | Yes |
| D069 | M002/S04 | convention | PeriodSelector reuse across analytics screens | Single `PeriodSelector` component in `components/analytics/` shared by AnalyticsScreen (D066 periods) and ExerciseDetailScreen (D048 periods) | Avoids duplicating the pill selector pattern. Component accepts `periods` array prop — callers define their own period options. | No |
| D070 | M003 | arch | Feed architecture — hybrid denormalization | `feedItems` table with denormalized workout snapshot, created on workout completion in `finishWorkout`. Feed query reads only `feedItems` (single indexed table), not all followed users' workouts. | Pure fan-out-on-read (query all followed users' workouts) causes excessive Convex subscription recomputation. Pure fan-out-on-write (pre-compute per-follower rows) is overkill at this scale. Hybrid: write one `feedItem` per event, read by querying feedItems where authorId ∈ followed users. Keeps feed query bounded and reactive. | Yes |
| D071 | M003 | arch | Profile data in Convex, not just Clerk | Dedicated `profiles` table in Convex storing username, bio, avatarStorageId, isPublic, stats. Seeded from Clerk identity on first visit. | Clerk stores auth identity but not app-level profile data (bio, username, privacy settings). Convex table enables indexing, search, and realtime queries on profile data. Clerk `getUserIdentity()` seeds initial name/avatar. | No |
| D072 | M003 | convention | Username storage — case-insensitive uniqueness | Store `username` (display case) and indexed `usernameLower` (lowercase). Unique index on `usernameLower`. 3-30 chars, alphanumeric + underscore. | Convex indexes are case-sensitive. Storing lowercase separately enables case-insensitive uniqueness without custom collation. Display case preserved for profile URLs and UI. | No |
| D073 | M003 | convention | Workout privacy default | Default public (`isPublic: true` via app logic, stored as `v.optional(v.boolean())` on workouts table) | Matches fitness app conventions (Strava, Hevy). Maximizes social content in feeds. Users can toggle per-workout or set global preference. `v.optional` means existing workouts (pre-M003) are treated as public. | Yes |
| D074 | M003 | convention | Feed items are immutable snapshots | Feed items store a denormalized `summary` object (workout name, duration, exercise count, PR count) at creation time. Not updated if workout is later edited. | Simpler than live-fetching workout data for every feed render. A shared workout summary is a point-in-time record. If workout is deleted, feed item is cascade-deleted. Avoids stale reference reads. | Yes |
| D075 | M003 | convention | Public share via Next.js route + Convex public query | `/shared/[id]` route uses a Convex query without `getUserId()` check. No HTTP actions needed. Route excluded from Clerk middleware. | Convex queries without auth checks are functionally public. Next.js dynamic route handles SSR and OG meta. Avoids adding an `http.ts` router that doesn't exist in the project. | No |
| D076 | M003 | convention | Avatar storage via Convex file storage | Upload URL → `ctx.storage.store()` → storage ID stored in `profiles.avatarStorageId` → `ctx.storage.getUrl()` for serving | Keeps entire data layer in Convex. No external storage service needed. Old avatar storage IDs cleaned up on avatar update to prevent orphans. | Yes |
| D077 | M003 | convention | Web-first, mobile port last (M003) | S01-S03 deliver backend + web UI. S04 ports all social features to mobile. | Continues D050 pattern from M002. Backend queries are shared — mobile only needs UI components. Isolates mobile navigation redesign to one slice. | No |
| D078 | M003 | convention | Mobile tab consolidation for social | Profile tab replaces Settings (settings nested as sub-screen inside Profile). Feed tab added. Target: 6 tabs (Exercises, Workouts, Templates, Analytics, Feed, Profile). | 7 tabs is too crowded. Settings is the least-used tab and naturally fits as a sub-screen of Profile. 6 tabs is the maximum for comfortable bottom-tab navigation. | Yes |
| D079 | M003 | convention | Multi-user test helpers | Extend `testing.ts` with social test helpers (`testCreateProfile`, `testFollowUser`, `testCreateFeedItem`, etc.) accepting explicit `testUserId` strings for multi-user scenarios | Existing pattern (D017) works for single-user tests. Social features need 2+ test users interacting. Same approach — internal mutations with explicit userId args — scales to multi-user. | No |
| D080 | M003/S01 | convention | Username immutability | Usernames cannot be changed after profile creation. `updateProfile` does not accept `username` arg. | Prevents URL breakage at `/profile/[username]`. Avoids cascading updates to feed items, follows, etc. Simple and safe for MVP. | Yes |
| D081 | M003/S01 | convention | Streak computation uses UTC days | Current streak counts consecutive UTC days with at least one completed workout, walking backward from today. No timezone awareness. | Timezone-aware streaks add significant complexity (user timezone storage, DST handling). UTC is accurate enough for MVP. May show off-by-one for users near midnight in non-UTC zones. | Yes |
| D082 | M003/S01 | convention | Profile page data attributes | `data-profile-setup-form`, `data-profile-stats`, `data-profile-page` attributes on profile UI containers | Extends D057/D064 pattern. Enables programmatic browser assertions without fragile text/class selectors. | No |
| D083 | M003/S01 | convention | Profile stats via computePeriodSummary reuse | `getProfileStats` reuses `computePeriodSummary(db, userId, undefined)` for all-time aggregates (totalWorkouts, totalVolume, topExercises) + new streak computation | DRY — same aggregation logic, different time window (undefined = all-time). Streak is the only new computation. | No |
| D084 | M003/S01 | convention | computePeriodSummary accepts undefined periodDays | Changed `periodDays: number` → `periodDays: number | undefined` in `computePeriodSummary`. When undefined, no time cutoff is applied (all-time query). Filter uses `cutoff === undefined || w.completedAt >= cutoff`. | Enables profile stats all-time aggregation without passing a huge number. Backward compatible — existing callers pass number values unchanged. | No |
| D085 | M003/S01 | convention | Conditional Convex query with "skip" | Use `useQuery(api.fn, condition ? args : "skip")` to conditionally execute Convex queries — skip when dependent data (userId, profile) is not yet loaded | Avoids unnecessary queries and errors when upstream data is still loading. Pattern used in profile setup (skip getProfile until user.id loaded) and profile view (skip getProfileStats until profile loaded). Standard Convex pattern. | No |
| D086 | M003/S01 | convention | Inline edit on profile view page | Own-profile editing uses inline form toggle on the view page rather than modal or separate /edit route | Simplest approach for S01 scope — avoids navigation overhead and modal complexity. Toggle between display and edit mode in-place. Can be upgraded to modal/page later if UX demands it. | Yes |
| D087 | M003/S02 | convention | Feed query: single-table paginate + post-filter | `getFeed` paginates full `feedItems` table by `createdAt` desc, post-filters for `authorId ∈ followedIds AND authorId ∉ blockedIds AND isPublic`. Not per-user merge. | Per-user merge requires N queries (one per followed user) and can't use `.paginate()`. Single-table approach uses one paginated query with reactive invalidation on one table. Post-filter cost bounded by page size (~50 items per page scan). | Yes |
| D088 | M003/S02 | convention | Reaction types: 5 emoji set | fire, fistBump, clap, strongArm, trophy — validated via `v.union(v.literal(...))` | Social fitness app standard emoji set. Validates in mutation to prevent arbitrary types. One reaction per type per user per feed item (unique constraint via composite index). | Yes |
| D089 | M003/S02 | convention | Block cascade removes follows | `blockUser` deletes follow relationships in both directions (blocker→blocked and blocked→blocker) | Prevents blocked user's old content from lingering in feed. Clean break semantics — blocking fully disconnects the social relationship. | No |
| D090 | M003/S02 | convention | Feed data attributes | `data-feed-page`, `data-feed-item`, `data-reaction-bar`, `data-follow-button` on social UI containers | Extends D057/D064/D082 pattern. Enables programmatic browser assertions for feed, reactions, and follow UI without fragile selectors. | No |
| D091 | M003/S02 | convention | Single social.ts module | All follow, feed, reaction, block, report functions in one `social.ts` file rather than split across follows.ts/feed.ts/reactions.ts/blocks.ts | Simpler file structure for S02 scope (~11 functions). Functions share imports (auth, schema types). Can be split later if file grows beyond ~400 lines. | Yes |
| D092 | M003/S02 | convention | ReactionBar local optimistic state overlay | ReactionBar uses local state Map (per-type `{userHasReacted, countDelta}`) overlaying server data, auto-cleared on mutation success/failure. Not Convex optimistic updates. | Simpler code with reliable revert on error. Convex optimistic updates require update function that mirrors server logic — overkill for a toggle. Local overlay is transparent and self-healing. | Yes |
| D093 | M003/S02 | convention | FollowButton self-contained component | `FollowButton` manages its own follow status query, counts query, mutations, and hover state. Shows "Follow" / "Following" / "Unfollow" (on hover). | Encapsulates all follow interaction logic in one component — reusable on any page that shows another user's profile. Hover-to-unfollow matches Twitter/X and Instagram patterns. | Yes |
| D094 | M003/S02 | convention | Feed pagination via usePaginatedQuery status | Feed page uses `usePaginatedQuery` with 5 status-based UI states: LoadingFirstPage (spinner), empty (discovery prompt), CanLoadMore ("Load More" button), LoadingMore (spinner), Exhausted ("all caught up"). | Clean separation of all possible feed states. `usePaginatedQuery` is Convex's built-in pagination hook — returns `{ results, status, loadMore }`. Status-driven rendering avoids boolean flag combinatorics. | No |
| D095 | M003/S03 | convention | Separate sharing.ts module | New `sharing.ts` file for share/clone/privacy functions rather than adding to social.ts or workouts.ts | social.ts already has 11 functions (D091). Sharing is a distinct domain (public queries, clone flow, privacy cascade). Clean separation keeps each module focused. | Yes |
| D096 | M003/S03 | convention | computePeriodSummary includePrivate parameter | `computePeriodSummary` gains `includePrivate: boolean = true` parameter. When `false`, filters `w.isPublic !== false`. Default `true` preserves backward compatibility for all existing callers. | Only `getProfileStats` needs private-excluded stats (cross-user visibility). All other callers (own analytics, weekly/monthly summaries) should see all workouts including private. Default-true means only one caller changes behavior. | No |
| D097 | M003/S03 | convention | Share token = feedItem._id | Use `feedItem._id` as the share token in `/shared/[id]` URL. No separate random token generation. Convex IDs are opaque/unguessable strings. | Simpler than generating UUIDs — avoids new field on feedItems. `shareWorkout` creates a `workout_shared` feedItem which serves as both the share record and the URL token. | No |
| D098 | M003/S03 | convention | Defense-in-depth privacy check on shared workout | `getSharedWorkout` checks BOTH `feedItem.isPublic` AND `workout.isPublic !== false`. Even if feedItem.isPublic is stale (user toggled privacy after sharing), the workout check catches it. | Prevents privacy leaks from stale feed items. `toggleWorkoutPrivacy` cascade-updates feed items (option A from research), and getSharedWorkout also checks the workout (option B). Belt and suspenders. | No |
| D099 | M003/S03 | convention | Shared workout page data attributes | `data-shared-workout`, `data-clone-button`, `data-share-button`, `data-privacy-toggle` on sharing UI containers | Extends D057/D064/D082/D090 pattern. Enables programmatic browser assertions for sharing features without fragile selectors. | No |
| D100 | M003/S04 | convention | Social components in social/ subdirectory | All 8 native social components placed in `apps/native/src/components/social/` subdirectory, suffixed with `Native` (D067) | Keeps social components grouped and discoverable. Avoids cluttering the top-level components directory which already has 15+ files. Matches analytics/ subdirectory pattern. | Yes |
| D101 | M003/S04 | convention | FeedScreen infinite scroll guard pattern | `onEndReached` calls `loadMore` only when `status === "CanLoadMore"` — no additional debounce or threshold | FlatList fires onEndReached multiple times rapidly. The status check is the canonical guard per Convex docs. Additional debouncing adds complexity with no benefit since `usePaginatedQuery` already deduplicates in-flight requests. | Yes |
| D102 | M003/S04 | convention | Profile-exists guard in ProfileScreen, not navigation | Profile-exists check (`useQuery(api.profiles.getProfile)` returns null) handled inside ProfileScreen with a "create profile" prompt rather than a navigation-level redirect | Simpler than a navigation middleware. ProfileScreen can show a nice inline prompt. Avoids complex navigation guard logic that would need to run before every tab switch. | Yes |
| D103 | M003/S04 | convention | Settings consolidation into ProfileScreen — section below profile | Settings section (unit toggle, rest timer, sign out) rendered as a section below profile info in ProfileScreen's ScrollView | Natural grouping — profile and preferences are both "about me" content. Matches Strava/Hevy pattern where settings are nested under profile. Preserves all SettingsScreen functionality (D078). | No |
| D104 | M003/S04 | convention | expo-clipboard version ~55.0.8 | Used `expo-clipboard` ~55.0.8 (latest stable) instead of ~7.0.3 from initial plan (which doesn't exist on npm) | expo-clipboard follows Expo SDK versioning (SDK 55 → ~55.0.x). The ~7.x version referenced in early planning was incorrect. Always match the Expo SDK major version. | No |
| D105 | M003/S04 | convention | EnrichedFeedItem explicit type for FlatList | Defined `EnrichedFeedItem` interface + `FlatList<EnrichedFeedItem>` generic to resolve pagination return type inference in native app | Convex's `usePaginatedQuery` returns enriched items (with author, summary, reactions) but TypeScript can't infer the full type when `convex/react` module resolution fails in native tsconfig. Explicit type provides compile-time safety and FlatList key extraction. | Yes |
| D106 | M003/S04 | convention | ProfileSetupScreen uses CommonActions.reset on success | After profile creation, `CommonActions.reset` replaces navigation stack instead of `navigation.navigate` | Prevents user from pressing back to return to setup screen after completing profile creation. Clean stack reset is the standard pattern for one-time setup flows. | No |
| D107 | M004 | arch | Pre-computed leaderboard entries over on-demand aggregation | `leaderboardEntries` table with indexed scores, updated via non-fatal hooks in `finishWorkout`, not computed on query | On-demand ranking across all users would scan unbounded documents and exceed Convex query limits. Pre-computed entries enable O(1) top-N via `.withIndex().order("desc").take(N)`. Convex Aggregate component deferred — would require `convex.config.ts` setup the project doesn't have. | Yes |
| D108 | M004 | arch | Leaderboard update on finishWorkout, not per-set | Leaderboard entries updated in `finishWorkout` (workout completion), not in `logSet` (per-set) | Per-set updates add write cost to the hottest mutation path. Workout completion is the natural aggregation point — same pattern as feed item creation (D070). Challenge standings may need per-set updates for realtime feel but are independently non-fatal. | Yes |
| D109 | M004 | convention | Leaderboard opt-in default false | `leaderboardOptIn: v.optional(v.boolean())` on profiles table, default false (undefined = not opted in) | Respects user privacy — users must explicitly choose to appear on public leaderboards. Opposite of D073 (public workout default) because ranking exposure is more sensitive than workout visibility. | No |
| D110 | M004 | convention | Hardcoded badge definitions, not data-driven rules engine | Badge definitions as a TypeScript constant object (`BADGE_DEFINITIONS`) with ~15-20 badges, not a database-driven rules table | 15-20 badges don't justify a rules engine parser. TypeScript constant is type-safe, easily testable, and deployable without data migration. Revisit if badge catalog exceeds 50. | Yes |
| D111 | M004 | arch | Challenge lifecycle via cron jobs + ctx.scheduler.runAt | `crons.ts` for periodic challenge checks (every 15 min), `ctx.scheduler.runAt(endTime)` for precise deadline scheduling on challenge creation | First use of Convex scheduled functions in the project. Cron for consistency safety net, `runAt` for timely completion. Completion function is idempotent — safe to fire multiple times. | No |
| D112 | M004 | convention | Absolute leaderboards only, no Wilks/DOTS normalization | Leaderboards rank by absolute weight/volume/reps. No bodyweight normalization formulas. Simple weight class filtering deferred. | DOTS/Wilks requires user bodyweight data (not in schema), adds formula complexity, and provides marginal benefit at early competitive feature scale. Absolute ranking is clear and understandable. | Yes |
| D113 | M004 | convention | Slice ordering — leaderboards first, mobile port last | S01 Leaderboards → S02 Challenges → S03 Badges → S04 Mobile. Backend + web first per D050/D077 pattern. | Leaderboards prove the pre-computed ranking pattern that challenges depend on. Badges are self-contained. Mobile consumes shared backend — isolated to one slice. Continues established web-first convention. | No |
| D114 | M004 | convention | "Show my rank" via bounded scan | User's own leaderboard rank computed by `.withIndex().order("desc").take(1000)` and finding their entry. If not in top 1000, show "Not ranked in top 1000". | Avoids Convex Aggregate component dependency for `indexOf`. Bounded scan is O(1000) worst case — acceptable for MVP. Can upgrade to Aggregate if rank precision becomes a hard requirement. | Yes |
| D115 | M004 | convention | Challenge participant cap of 100 | Maximum 100 participants per challenge to bound standings query cost | `challengeParticipants` query with `.take(100)` keeps standings computation bounded. 100 participants is generous for friend-group challenges. Can increase with pre-aggregation if needed. | Yes |
| D116 | M004 | convention | Challenge types: 4 initial types | totalReps, totalVolume, workoutCount, maxWeight — covering the main competitive patterns | Matches R019 examples. Each type has a clear metric computation from existing set/workout data. Additional types (e.g., streak-based) can be added later. | Yes |
| D117 | M004/S01 | convention | Leaderboard entries store allTime period only in S01 | Store only `period: "allTime"` entries. Time-filtered views (7d/30d) use `updatedAt` field comparison at query time. Avoids tripling row count with period-specific entries. | Simpler schema, bounded write cost on finishWorkout. S02 can add cron-based period-specific entries if needed. Query-time filtering is acceptable at MVP scale. | Yes |
| D118 | M004/S01 | convention | Leaderboard data attributes | `data-leaderboard-page`, `data-leaderboard-table`, `data-leaderboard-rank`, `data-leaderboard-optin`, `data-leaderboard-exercise-select` on leaderboard UI containers | Extends D057/D064/D082/D090/D099 pattern. Enables programmatic browser assertions for leaderboard features without fragile selectors. | No |
| D119 | M004/S01 | convention | Leaderboard metric enum values | Metric values stored as "e1rm", "volume", "reps" (lowercase, matching PR type naming convention) | Consistent with existing `prType` convention in schema. Short, readable, unambiguous. | No |
| D120 | M004/S01 | convention | Leaderboard opt-in filter strategy — take limit×3 | `getLeaderboard` fetches `limit * 3` entries then post-filters by profile `leaderboardOptIn`. Returns first `limit` opted-in entries. | Opt-in status lives on profiles table, not leaderboardEntries — can't include in index. 3x overfetch handles typical opt-in ratios. Acceptable for MVP. | Yes |
| D121 | M004/S02 | convention | Challenge progress via incremental delta, not full recompute | `updateChallengeProgress` in finishWorkout computes only the current workout's contribution (delta) and adds it to `currentValue`. For max types (maxWeight), compare-and-update only if greater. Never recomputes from full set history. | Keeps computation bounded per workout (O(sets in this workout × active challenges)). Avoids hitting Convex 16K doc read limit for long challenges with prolific users. Tradeoff: stale `currentValue` after workout deletion — documented as known limitation. | Yes |
| D122 | M004/S02 | convention | Challenge test helpers skip ctx.scheduler | `testCreateChallenge` does not call `ctx.scheduler.runAt` — verification script triggers `testCompleteChallenge` and `testActivateChallenge` directly. Cron scheduling is trusted infrastructure; only the called functions are tested. | Convex scheduled functions can't be triggered in test helpers without a live deployment's scheduler. Testing the functions they call proves correctness. Scheduling is Convex platform responsibility. | No |
| D123 | M004/S02 | convention | Challenge detail as inline expansion, not separate route | Challenge detail (standings, info, actions) renders inline on `/challenges` page when a card is clicked — no `/challenges/[id]` dynamic route. | Simpler routing. Single-page interaction feels snappier. Challenge list and detail share the same Convex subscriptions. Can add dedicated route later if deep linking is needed. | Yes |
| D124 | M004/S02 | convention | Challenge data attributes | `data-challenge-page`, `data-challenge-list`, `data-challenge-detail`, `data-challenge-standings`, `data-challenge-create`, `data-challenge-join` on challenge UI containers | Extends D057/D064/D082/D090/D099/D118 pattern. Enables programmatic browser assertions for challenge features without fragile selectors. | No |
| D125 | M004/S02 | convention | Creator auto-joins challenge on creation | `createChallenge` automatically inserts a `challengeParticipants` entry for the creator with `currentValue: 0`. Creator cannot leave their own challenge (must cancel instead). | Natural UX — creator is always a participant. Prevents empty challenges. Matches Strava/Fitbit challenge patterns. `leaveChallenge` rejects if userId === creatorId. | No |
| D126 | M004/S02 | convention | checkDeadlines processes transitions inline, not via scheduler | `checkDeadlines` internal mutation directly patches challenge status (active→completed, pending→active) inline rather than calling `ctx.scheduler.runAfter(0, internal.challenges.completeChallenge)`. | Avoids scheduler overhead for immediate transitions. The cron is already executing at the right time. Inline processing is simpler and keeps the mutation atomic. Winner determination happens in the same transaction. | Yes |
| D127 | M004/S02 | convention | Stale currentValue on workout deletion is a known limitation | Challenge participant `currentValue` is not recomputed when a contributing workout is deleted. Value may be inflated. | Recomputing requires traversing all sets for all active challenges — expensive and complex. Documented as known limitation for S02 MVP. Cron-based recomputation or delete-time recomputation can be added later. Challenges are time-limited so staleness is bounded. | Yes |
| D128 | M004/S03 | convention | Badge definitions as framework-agnostic TypeScript constant in convex/lib/ | `BADGE_DEFINITIONS` in `convex/lib/badgeDefinitions.ts` — plain TypeScript object with no React/Convex server imports. Both backend evaluation and web/mobile UI import it for display metadata (emoji, name, description). | Same pattern as `muscle-heatmap-paths.ts` (D063). Single source of truth importable from both backend (`../lib/badgeDefinitions`) and web (`@packages/backend/convex/lib/badgeDefinitions`). | Yes |
| D129 | M004/S03 | convention | Badge evaluation batch-fetches all stats once, iterates definitions | `evaluateAndAwardBadges` queries 5 aggregates (workoutCount, totalVolume, currentStreak, prCount, completedChallengeCount) once at top, fetches existing badges into a Set, then iterates `BADGE_DEFINITIONS` checking thresholds. No per-badge queries. | Minimizes query count (~5-6 indexed reads). Non-fatal wrapper bounds risk. Self-healing: missed badges caught on next workout completion. | No |
| D130 | M004/S03 | convention | Badge data attributes | `data-badge-section`, `data-badge-card`, `data-badge-slug` on badge UI containers | Extends D057/D064/D082/D090/D099/D118/D124 pattern. Enables programmatic browser assertions for badge features without fragile selectors. | No |
| D131 | M004/S03 | convention | Badges section placement on profile page | Badge section renders between leaderboard opt-in toggle and workout stats for all profile views (own and others) | Badges are always public — no opt-in needed. Placement keeps profile compact without pushing stats off-screen (research constraint). Visible before scrolling on typical viewports. | Yes |
| D132 | M004/S04 | convention | Generic PillSelectorNative instead of modifying PeriodSelector | New `PillSelectorNative<T extends string>` generic component in `components/competitive/` rather than modifying existing PeriodSelector to accept string values | PeriodSelector is used by analytics screens with `value: number | undefined`. Changing its type signature risks breaking existing callers. A separate generic component is safer and more flexible. | Yes |
| D133 | M004/S04 | convention | Compete tab as 7th bottom tab with CompeteStack | Single "Compete" tab (7th) containing a stack navigator with ChallengesScreen as landing, LeaderboardScreen, and ChallengeDetailScreen as push targets | Both leaderboards and challenges are discovery experiences that don't fit under existing tabs. 7 tabs is the established ceiling (D078). ChallengesScreen as landing page (higher engagement surface). | Yes |
| D134 | M004/S04 | convention | ChallengeDetailScreen as separate stack screen, not inline expansion | Mobile challenge detail pushes to ChallengeDetailScreen via stack navigation, unlike web which uses inline expansion (D123) | Inline expansion in FlatList is awkward on mobile (scroll position jumps, no URL routing). Stack push/pop is the natural mobile pattern. | No |
| D135 | M004/S04 | convention | Simple exercise selector for challenge creation instead of reusing ExercisePicker | Challenge create form uses `useQuery(api.exercises.listExercises)` in a Modal with ScrollView rather than the existing ExercisePicker component | ExercisePicker is tightly coupled to workoutId (calls `addExerciseToWorkout` mutation on select). Decoupling requires refactoring existing component; simpler to build a lightweight selection modal. | Yes |
| D136 | M004/S04 | convention | TextInput date entry for challenge creation with YYYY-MM-DD HH:MM format | Challenge start/end dates use plain TextInput with "YYYY-MM-DD HH:MM" placeholder and `new Date()` parsing, not a native date picker | No `@react-native-community/datetimepicker` dependency in project. Plain TextInput avoids new dependency for MVP. Can upgrade later if UX friction is high. | Yes |
| D137 | M005 | arch | Separate sessionParticipants table, not embedded in groupSessions | Per-participant documents linked to session via `sessionId` foreign key, rather than a single session document with embedded participant array | Reduces write contention — participants send heartbeats independently without locking the session document. Convex subscriptions scope to participant documents, reducing unnecessary refires on session-level changes. Mirrors `challengeParticipants` pattern from D111. | No |
| D138 | M005 | arch | Server-authoritative shared timer via stored timerEndAt | Timer state stored as `sharedTimerEndAt` and `sharedTimerDurationSeconds` on `groupSessions` document. Clients compute `remaining = timerEndAt - Date.now()` locally. Deliberate departure from D008 (client-only timer). | Client-only timers drift between participants. Server timestamp is the only shared reference point. `Date.now()` is frozen per mutation (Convex guarantee) so `timerEndAt = Date.now() + duration * 1000` is deterministic. NTP clock skew bounds drift to <1 second. | No |
| D139 | M005 | convention | Heartbeat frequency 10 seconds, presence threshold 30 seconds | Heartbeat mutation every 10 seconds per participant. Cron runs every 30 seconds to mark participants with `lastHeartbeatAt > 30s ago` as idle. | 10s heartbeat halves write cost vs 5s with acceptable 40-second worst-case detection delay (heartbeat interval + cron interval). For ≤10 participants, 60 heartbeat writes/minute per session is well within Convex limits. | Yes |
| D140 | M005 | convention | Shared timer last-write-wins | Any participant can start/pause/skip the shared timer. No permission hierarchy. Mutation unconditionally overwrites `sharedTimerEndAt`. | Simplest UX for training partners — either person hits the timer, like a real gym. Avoids complex locking or ownership model. Research recommended this approach. | Yes |
| D141 | M005 | convention | Session host-only end/cancel, auto-end on timeout | Only the session creator (host) can end or cancel a session. Sessions with no heartbeats from any participant for 15 minutes auto-complete via cron. | Prevents grief-ending by non-hosts. Auto-timeout handles abandoned sessions without manual intervention. Same single-authority pattern as challenge creator (D125). | Yes |
| D142 | M005 | convention | Dedicated sessions.ts module | New `sessions.ts` file for all group session functions, not added to workouts.ts or social.ts | workouts.ts already has 4 non-fatal hooks (growing complexity). sessions.ts is a distinct domain with its own lifecycle. Follows D091/D095 convention of domain-specific modules. | No |
| D143 | M005 | convention | Per-participant workout records with sessionId foreign key | Each participant gets an individual `workouts` row with `sessionId` linking to `groupSessions`. Set logging uses the normal `logSet` path, triggering PR detection, leaderboard updates, etc. | Ensures group workout data flows into all existing systems (analytics, PRs, leaderboards, badges) without special-casing. The session view aggregates across participants' individual workouts. | No |
| D144 | M005 | convention | Session route at /workouts/session/[id], join at /session/join/[id] | Live session page nested under workouts (existing protected route). Join link at /session/join/[id] as a new protected route. | Deep-linkable session URLs. Join route is separate because it handles the join flow before redirecting to the session page. Both Clerk-protected (authenticated users only — D075 public pattern not used since sessions require auth). | Yes |
| D145 | M005 | convention | Web-first, mobile port last (M005) | S01-S03 deliver backend + web UI. S04 ports to mobile within existing Workouts tab stack. | Continues D050/D077/D113 convention. Backend queries are shared — mobile only needs screen components. Group workout flows nest within Workouts tab (D133 7-tab ceiling). | No |
| D146 | M005 | convention | Invite code as 6-char random string for short URLs | `inviteCode` field on `groupSessions` with unique index. Generated server-side in `createSession`. Join link uses `/session/join/[inviteCode]` for shorter, shareable URLs. | Convex `_id` is long and opaque. A 6-char alphanumeric code (~2.2 billion combinations) is human-readable and shareable via text/voice. Uniqueness enforced by index. Research recommended this approach. | Yes |
| D147 | M005 | convention | Participant limit hard cap at 10 | `joinSession` rejects if session already has 10 participants | Performance bound — 10 participants × 10-second heartbeat = 60 writes/minute. Session queries reading all participants' sets stay within Convex 10s query timeout. Context document specifies ≤10 optimization target. | Yes |
| D148 | M005 | convention | Late join allowed for active sessions | Users can join sessions in "waiting" or "active" status. Late joiners see ongoing workout state mid-stream. | Improves usability — friends arriving late at the gym can still join. Session query returns all current state. Research recommended yes for usability. | Yes |
